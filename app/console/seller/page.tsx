import SellerPage, { metadata as sellerMetadata } from "@/app/seller/page";

export const metadata = {
  ...sellerMetadata,
  title: "Services / Seller | Developer Console | Arc Agent Commerce",
};

export default SellerPage;
